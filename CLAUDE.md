# lab

작은 웹 서비스 모음입니다. 서비스마다 `apps/<이름>/` 아래 독립된 Next.js 앱이 있고, 앱별 규칙은 각 앱의 `CLAUDE.md`를 따릅니다.

## 작업 흐름

묻지 않고 끝까지 진행합니다.

1. 작업이 끝나면 커밋하고 작업 브랜치에 **푸시합니다.**
2. 푸시한 브랜치에 열린 PR이 없으면 `main`을 향한 **PR을 만듭니다.**
3. 검증을 통과하면 PR을 **`main`에 합칩니다** (squash merge).

### 합치기 전 검증

- 바뀐 앱마다 `npx tsc --noEmit`과 `npm run build`를 실행해 통과하는지 확인합니다
- 화면이 바뀌었으면 앱을 띄워 실제로 동작하는지 확인합니다
- PR에 CI 체크가 있으면 모두 초록일 때만 합칩니다
- PR에 해결되지 않은 리뷰 코멘트가 있거나 충돌이 나면 먼저 해결합니다

새 앱은 `main`에 합치면 `Vercel projects` 워크플로가 Vercel 프로젝트를 만들고 배포합니다. 합친 뒤 Actions에서 그 실행이 성공했는지 확인합니다.

검증에 실패하면 합치지 않습니다. 고칠 수 있으면 고쳐서 다시 검증하고, 고칠 수 없으면 무엇이 막혔는지 알립니다.
